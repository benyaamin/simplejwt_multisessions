name: CI

on:
  push:
    branches: ["main", "master"]
  pull_request:
    branches: ["main", "master"]

jobs:
  test:
    name: Python ${{ matrix.python-version }} / Django ${{ matrix.django-version }}
    runs-on: ubuntu-latest

    strategy:
      fail-fast: false
      matrix:
        include:
          # Django 3.2 LTS — Python 3.8, 3.9, 3.10
          - python-version: "3.8"
            django-version: "Django>=3.2,<4.0"
          - python-version: "3.9"
            django-version: "Django>=3.2,<4.0"
          - python-version: "3.10"
            django-version: "Django>=3.2,<4.0"
          # Django 4.2 LTS — Python 3.8–3.12
          - python-version: "3.8"
            django-version: "Django>=4.2,<5.0"
          - python-version: "3.10"
            django-version: "Django>=4.2,<5.0"
          - python-version: "3.12"
            django-version: "Django>=4.2,<5.0"
          # Django 5.2 LTS — Python 3.10–3.13
          - python-version: "3.10"
            django-version: "Django>=5.2,<6.0"
          - python-version: "3.12"
            django-version: "Django>=5.2,<6.0"
          - python-version: "3.13"
            django-version: "Django>=5.2,<6.0"

    steps:
      - uses: actions/checkout@v4

      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install "${{ matrix.django-version }}" \
                      djangorestframework \
                      djangorestframework-simplejwt

      - name: Run tests
        run: |
          DJANGO_SETTINGS_MODULE=test_settings python -m django test simplejwt_multisessions.tests -v 2
